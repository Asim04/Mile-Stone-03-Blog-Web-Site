import Container from '../Container/Container'; // Assuming this component is in a separate file

export default function My() {
  return (
    
    <Container>
        <h1>This is a heading inside the Container</h1>
        <p>This is a paragraph with consistent padding.</p>

    </Container>
  );
}

